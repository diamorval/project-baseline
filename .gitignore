# Node
node_modules/
frontend/dist/
*.log

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/

# Env: the root .env is the generated local config (cp from .env.example); the
# committed frontend/.env holds only public localhost URLs. /.env is root-anchored
# so it does NOT match frontend/.env.
/.env
.env.local
*.env.local

# direnv
.direnv/

# Claude Code — personal/local settings (the shared .claude/settings.json IS committed)
.claude/settings.local.json

# OS / editors
.DS_Store
.idea/
.vscode/

*-lock.yaml
