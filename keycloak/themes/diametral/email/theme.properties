# Diametral email theme — extends Keycloak's email theme and overrides only the
# shared HTML shell (html/template.ftl), so every transactional email (verify
# email, reset password, execute-actions, etc.) is wrapped in the Diametral
# layout without touching each individual template.
parent=keycloak
